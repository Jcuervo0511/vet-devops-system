apiVersion: apps/v1
kind: Deployment
metadata:
  name: vet-canary
  labels:
    app: vet-app
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vet-app
      version: canary
  template:
    metadata:
      labels:
        app: vet-app
        version: canary
    spec:
      containers:
        - name: vet-app
          image: 578673726717.dkr.ecr.us-east-2.amazonaws.com/devops:canary
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: vet-app-secret
                  key: DATABASE_URL
            - name: NODE_ENV
              value: "production"
